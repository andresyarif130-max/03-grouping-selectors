# 03-grouping-selectors

Learning grouping selectors helps apply the same CSS styles to multiple elements at once. This practice makes the code cleaner, reduces repetition, and improves efficiency when styling a webpage.